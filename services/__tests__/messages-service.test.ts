import MessagesService from '../MessagesService';

describe('MessagesService', () => {
    // test('', async () => {
    //     const result = await MessagesService.createChat();
    //     expect(result);
    // });

    // test('', async () => {
    //     const result = await MessagesService.likeMessage();
    //     expect(result);
    // });

    // test('', async () => {
    //     const result = await MessagesService.sendMessage();
    //     expect(result);
    // });

    // test('', async () => {
    //     const result = await MessagesService.unlikeMessage();
    //     expect(result);
    // });
});

