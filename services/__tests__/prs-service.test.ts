import PRsService from '../PRsService';

describe('PRsService', () => {
    // test('', async () => {
    //     const result = await PRsService.upsertPRs();
    //     expect(result);
    // });

    // test('', async () => {
    //     const result = await PRsService.removePR();
    //     expect(result);
    // });

    // test('', async () => {
    //     const result = await PRsService.removePRs();
    //     expect(result);
    // });
});